Daily Log — 2026-03-22
Govern.1 Marker 4 — pipeline.jsonl Structured Logging
Session duration: ~4 hours
Status: ✅ Complete. Pending commit.

What Was Built
New files:

src/common_func/pipeline_logger.py — write_jsonl_entry(), read_recent_jsonl(), auto-captures run_name from Prefect context
src/common_func/metrics_contract.py — TypedDict schemas: KNMICollectMetrics, ZigbeeCollectMetrics, BronzeMetrics, SilverMetrics, GoldMetrics, AISummaryMetrics

Modified files:

config.py — added JSONL_RUNS_TO_READ=5, JSONL_LINES_PER_RUN=8
collect_data_knmi.py — wired KNMICollectMetrics
collect_zigbee_files.py — wired ZigbeeCollectMetrics, added raise, fixed client scope
collect_data_zigbee.py — wired error capture
load_knmi_to_bronze.py — wired BronzeMetrics
load_zigbee_to_bronze.py — wired BronzeMetrics
transform_knmi_to_silver.py — wired SilverMetrics
transform_zigbee_to_silver.py — wired SilverMetrics
transform_silver_to_gold.py — wired GoldMetrics
ai_summary.py — replaced read_recent_logs() with read_recent_jsonl(), prompt updated with relationship rules
pipeline_flow.py — removed unused imports, fixed f-string

Validated:

7 of 8 stages producing clean JSONL entries
dq_pass_rate, run_name, stage-specific metrics all confirmed
flake8 clean on all modified files


Key Decisions Made

Name by responsibility not format → pipeline_logger.py not jsonl_logs_util.py
Contracts govern metrics schema → metrics_contract.py separate from contract.py
Prompts encode relationships not values → LLM calculates gold_rows = knmi × zigbee
Bronze raw-as-is → station filtering belongs in Silver only
JSONL_RUNS_TO_READ=5, JSONL_LINES_PER_RUN=8 → reads last 40 lines (~10KB)
Enterprise contract path → TypedDict → versioned package → DataHub/Atlan


Bugs Found And Fixed

Duplicate collect_knmi() definition — silent wrong behaviour
write_jsonl_entry inside file loop — multiple entries per run
all_rows vs rows variable name
Import order wrong — sys.path.insert after local imports
dq_pass_rate calculated but not passed to GoldMetrics
Duplicate build_prompt — caused Ollama timeout
start_time scope issue in collect_zigbee_files.py