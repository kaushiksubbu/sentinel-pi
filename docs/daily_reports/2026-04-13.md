Session: Govern.2 Week 3 — Architecture + ADR Day
Duration: ~45 minutes
Type: No code. Debates and design.

Decisions Made:

1. Marquez ARM64 rejected permanently
   Capability preserved via OpenLineage → ops.db
   ADR-033 updated

2. dbt integration pattern resolved
   Three-tier scheduling locked
   Single Gold table. Single source of truth.
   Two Gold tables explicitly rejected — splits trust
   ADR-042 filed

3. lineage_events sink changed
   JSONL rejected — ops.db is single governance store
   Schema defined. Six emit points. Both raw sources.
   ADR-043 filed

Debates Resolved:
- OpenLineage vs pipeline.jsonl → different questions, both kept
- dbt on Gold → valid but not urgent, Python owns state
- Python transform vs dbt → adjacent jobs, not duplicate jobs
- Real-time vs certified Gold → correctness before freshness
- JSONL lineage sink vs DuckDB → DuckDB wins, JOINable governance

Open Items Carried Forward:
- Gate 4 (AI summary cleanup) — deprioritised, still debt
- BL-007, BL-008, BL-010 — deferred out of Week 3

Next Session:
- Gate 1 execution — BL-001, BL-002, BL-006, BL-009
- SSH only, no VSCode
- ~90 minutes