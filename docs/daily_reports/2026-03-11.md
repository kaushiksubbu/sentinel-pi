# Sentinel-Pi — Daily Log (2026-03-11)

## 🎉 PHASE 1 COMPLETE

---

## Platform Status — End of Day

| Layer | Status | Records |
|-------|--------|---------|
| Bronze KNMI | ✅ Automated 10-min | Growing |
| Bronze Zigbee | ✅ Automated 10-min | Growing |
| Silver KNMI | ✅ Live | 100% valid |
| Silver Zigbee | ✅ Live | 100% valid |
| Gold | ✅ Live | 1498+ rows, 100% valid |
| AI Summary | ✅ Live | Daily 08:30 report |

---

## What Was Built Today

### 1. FileLock Concurrency Control
- `filelock` library — OS-level atomic lock
- Lock metadata: PID + acquired_at + script name
- Parseable by AI summary for runtime observability
- Phase 2 Prefect supersedes — no rework needed

### 2. Config Consolidation — Complete
All constants in `config.py`:
```
BRONZE_LANDING, PROJECT_DIR, VENV_PYTHON
COLLECT_ZIGBEE_SCRIPT, KNMI_BASE_URL
BRONZE_ZIGBEE_TBL, KNMI_TBL
LOCK_FILE, LOCK_META
LOG_FILE, STRUCTURED_LOG_FILE
REPORTS_DIR, OLLAMA_URL, OLLAMA_MODEL
```

### 3. Code Quality — flake8 Clean
- `ingest_data.py` → clean
- `ai_summary.py` → clean
- `autopep8` used for automated fixes
- flake8 → Phase 2 GitHub Actions backlog

### 4. log_utils.py — Skeleton Created
- Dual output: cron.log + pipeline.jsonl
- Phase 2 tech debt — migrate all modules
- Microservice enablement path documented

### 5. AI Summary — Live
- `ai_summary.py` built and tested
- Reads: cron.log (last 10 lines) + gold_weather metrics
- Calls: Llama 3.2 1B instruct via Ollama API
- Saves: `docs/daily_reports/Log_Summary_YYYY-MM-DD.md`
- Cron: 08:30 daily

### 6. AI Model Evaluation — Complete
Full evaluation of 4 models on Pi 8GB:

| Model | Actual RAM | Time | Quality | Decision |
|-------|-----------|------|---------|----------|
| phi3:mini | 3.6GB | timeout | — | ❌ Rejected |
| Gemma:2b | 1.2GB | 4 mins | good | ❌ Too slow |
| TinyLlama | 800MB | 15-20s | hallucinated | ❌ Unreliable |
| Llama 3.2 1B instruct-q4_K_M | 600MB | <10s | accurate | ✅ Selected |

**Key lesson:** Advertised model size ≠ actual RAM under load.
Always measure actual footprint before committing.

### 7. Git Workflow — Professional Standard
```
git fetch → stash → rebase → stash pop → add → commit → push
```

---

## Key Decisions Today

### AI Model — Llama 3.2 1B instruct-q4_K_M
- 600MB actual RAM — fits Pi comfortably
- <10 seconds response on simple prompts
- 3-4 mins on full daily summary — acceptable
- 128K context window vs Gemma 8K — future-ready
- Meta pedigree — Dutch enterprise standard

### Two-Layer AI Design — Locked
```
Layer 1 → Python: exact metrics from Gold + logs
Layer 2 → LLM: narrative + pattern recognition
```
Phase 2 structured logging (pipeline.jsonl) feeds exact metrics.
Current Phase 1 — approximate metrics accepted.

### log_utils.py — Phase 2 Tech Debt
> "Debt reduction and microservice enablement task.
> Migrate all modules in Phase 2 when containerising."

### Prod/Dev Branch Strategy
Solved by Prefect Phase 2 — not worth solving with cron.

---

## Cron Schedule — Final

```
58 * * * *   → venv health check
*/10 * * * * → pipeline ingestion
30 8 * * *   → AI daily summary
```

---

## Known Limitations — Phase 1 AI Report

Performance numbers hallucinated — expected.
Root cause: unstructured cron.log input.
Fix: Phase 2 pipeline.jsonl structured logging.

---

## Capability Progress

**Capability 1 — Sensing** ✅ COMPLETE
**Capability 4 — Explanation** 🔄 Started (AI summary live)

---

## Phase 2 Preview

```
Week 2: Docker containerisation
Week 2: Prefect replaces cron
Week 2: pipeline.jsonl structured logging
Week 2: DPIA + ROPA governance docs
Week 2: LinkedIn + CV update
Week 3: gold_weather_features (ML feature store)
Week 4: Weather extremes prediction
```

---

## ADRs Added Today

```
ADR-025 → Dual Gold layer (reporting + ML feature store)
ADR-026 → AI model selection — Llama 1B instruct
ADR-027 → Two-layer AI design (Python + LLM)
```

---

## Tomorrow Entry Ticket

```bash
# Confirm overnight AI summary ran
cat /mnt/data/sentinel-pi/docs/daily_reports/Log_Summary_2026-03-12.md

# Gold row count
cd /mnt/data/sentinel-pi && \
/mnt/data/sentinel-pi/.venv/bin/python3 -c "
import duckdb
from config import GOLD_DB
con = duckdb.connect(GOLD_DB, read_only=True)
print(con.execute('''
    SELECT is_valid, COUNT(*),
    MIN(window_start), MAX(window_start)
    FROM gold_weather GROUP BY is_valid
''').fetchall())
"
```

Then — Phase 2 begins. Docker first.