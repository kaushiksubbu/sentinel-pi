Cron Monitoring & Correction
Identified misconfiguration due to user setup → corrected.

Logging Fixes
Updated modules for proper logging → ensures traceability, future DQ checks, and auditability.

Schema Handling
Genericized db_utils to create tables dynamically based on incoming data.

Problem: Zigbee JSON files were being loaded on every cron run → duplicates in Bronze tables.
Solution: Modified ETL to:
Write incoming files to landing zone
Load into Bronze DuckDB
Move successfully processed files to /processed folder

Impact:
Ensures idempotency of ingestion
Reduces tech debt and prevents data quality issues
Preserves auditability and traceability for every batch