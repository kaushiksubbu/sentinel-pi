decision made - Faster to market with working Silver — Parquet migration is moved as a backlog item, not a blocker.
new data process introduced for logging and watermarking.
# TODO: Phase 2 — Record level error handling
# Current: full rollback on any row failure
# Target: dead letter queue — failed rows isolated,
#         successful rows committed
# Blocked by: need to understand DuckDB savepoints